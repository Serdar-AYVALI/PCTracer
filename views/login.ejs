<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Giriş Yap - PCTracer</title>
    <link rel="shortcut icon" href="/img/favicon.ico">
        <!-- sekmede küçük bir simge görüntülenir.-->
    <link rel="stylesheet" href="/css/styles.css">
    
</head>

<body >

    <div class="wrapper">
    <div class="title-text">
        <div class="title login">Giriş Yap</div> <!--Başlıklar-->
        
    </div>
    <div class="form-container">
        <div class="form-inner">
            
            <form action="/user/login" method="POST" class="login" >
                <div class="field">
                    <input type="email" name="email" placeholder="E-Posta Adresi" required>
                </div>
                <div class="field">
                    <input type="password" name="password" placeholder="Parola" required>
                </div>
                <% if (typeof error !== 'undefined' && error) { %>
                <div class="login-error-message" style="display:flex;align-items:center;justify-content:center;gap:7px;color:#e74c3c;font-size:1.08em;font-weight:500;margin-bottom:14px;margin-top:10px;padding:0;min-height:unset;background:none;border:none;box-shadow:none;text-align:left;">
                    <svg style="vertical-align:middle;" width="18" height="18" fill="none" stroke="#e74c3c" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><path d="M12 8v4M12 16h.01"/></svg>
                    <span><%= error %></span>
                </div>
                <% } %>
                <div class="field btn">
                    <div class="btn-layer"></div>
                    <input type="submit" value="Giriş Yap">
                </div>
                <!--<div class="signup-link">Üye değil misin? <a href="signup.html">Şimdi Üye Ol</a></div>-->
            </form>
            <!--Giriş Kısmı-->
            
        </div>
    </div>


</body>

</html>